from typing import Any

# Lightweight local pytest type aliases to satisfy the static analyzer in tests.
# Keep permissive to avoid forcing precise pytest typing in test code.
MonkeyPatch = Any
RaisesContext = Any
